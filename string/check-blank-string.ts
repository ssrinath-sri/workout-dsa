/**
 * Checks if a given string is blank or not.
 * @param str The string to be checked.
 * @returns true if the string is blank, false otherwise.
 */
function isBlankString(str: string): boolean {
    return str.trim() === "";
}
