/**
 * Splits a given string into an array of words.
 * @param str The string to be split.
 * @returns An array of words.
 */
function stringToArrayOfWords(str: string): string[] {
    return str.split(/\s+/);
}
